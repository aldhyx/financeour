export const STORED_KEY = {
  /* user prefer language from storage */
  LANGUAGE: 'PREFER_LANGUAGE',
  /* user prefer color schema from storage */
  COLOR_SCHEMA: 'PREFER_COLOR_SCHEMA',
  /* stored db name & version, e.g: v1_test.db */
  DB_NAME: 'STORED_DB_NAME',
};
