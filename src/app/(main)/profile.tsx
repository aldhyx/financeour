import React from 'react';
import { View } from 'react-native';

import { Text } from '@/components/ui';

const ProfileScreen = () => {
  return (
    <View className="flex-1">
      <Text className="">ProfileScreen </Text>
    </View>
  );
};

export default ProfileScreen;
